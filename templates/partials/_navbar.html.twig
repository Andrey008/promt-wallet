<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <div class="container">
        <a class="navbar-brand" href="{{ path('app_home') }}">
            <i class="bi bi-journal-code me-2"></i>{{ app_name }}
        </a>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
            {% if app.user %}
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link {{ app.request.attributes.get('_route') starts with 'project_' ? 'active' : '' }}"
                       href="{{ path('project_index') }}">
                        <i class="bi bi-folder me-1"></i> Projects
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ app.request.attributes.get('_route') starts with 'context_' ? 'active' : '' }}"
                       href="{{ path('context_index') }}">
                        <i class="bi bi-file-text me-1"></i> Contexts
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ app.request.attributes.get('_route') starts with 'template_' ? 'active' : '' }}"
                       href="{{ path('template_index') }}">
                        <i class="bi bi-file-earmark-code me-1"></i> Templates
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ app.request.attributes.get('_route') starts with 'tag_' ? 'active' : '' }}"
                       href="{{ path('tag_index') }}">
                        <i class="bi bi-tags me-1"></i> Tags
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ app.request.attributes.get('_route') starts with 'snippet_' ? 'active' : '' }}"
                       href="{{ path('snippet_index') }}">
                        <i class="bi bi-code-square me-1"></i> Snippets
                    </a>
                </li>
            </ul>

            <div class="d-flex align-items-center">
                <a href="{{ path('compose_index') }}" class="btn btn-light me-3">
                    <i class="bi bi-magic me-1"></i> Compose Prompt
                </a>
                <button type="button" class="btn btn-outline-light btn-sm me-3" data-bs-toggle="modal" data-bs-target="#keyboardShortcutsModal" title="Keyboard Shortcuts (?)">
                    <i class="bi bi-keyboard"></i>
                </button>
                <div class="dropdown">
                    <a class="nav-link text-white dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                        <i class="bi bi-person-circle me-1"></i> {{ app.user.displayName }}
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end">
                        <li><span class="dropdown-item-text text-muted small">{{ app.user.email }}</span></li>
                        <li><hr class="dropdown-divider"></li>
                        <li>
                            <a class="dropdown-item" href="#" onclick="toggleTheme(); return false;">
                                <i id="theme-icon" class="bi bi-moon me-1"></i> Toggle Theme
                            </a>
                        </li>
                        <li>
                            <a class="dropdown-item" href="{{ path('api_tokens_index') }}">
                                <i class="bi bi-key me-1"></i> API Keys
                            </a>
                        </li>
                        <li>
                            <a class="dropdown-item" href="{{ path('import_export_export') }}">
                                <i class="bi bi-arrow-left-right me-1"></i> Import / Export
                            </a>
                        </li>
                        <li><hr class="dropdown-divider"></li>
                        <li>
                            <a class="dropdown-item" href="{{ path('app_logout') }}">
                                <i class="bi bi-box-arrow-right me-1"></i> Logout
                            </a>
                        </li>
                    </ul>
                </div>
            </div>
            {% else %}
            <ul class="navbar-nav ms-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_login') }}">
                        <i class="bi bi-box-arrow-in-right me-1"></i> Login
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_register') }}">
                        <i class="bi bi-person-plus me-1"></i> Register
                    </a>
                </li>
            </ul>
            {% endif %}
        </div>
    </div>
</nav>
